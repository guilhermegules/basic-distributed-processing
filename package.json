{
  "name": "basic-distributed-processing",
  "version": "1.0.0",
  "description": "basic distributed sum system",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r run build",
    "start:producer": "pnpm --filter producer start",
    "start:worker": "pnpm --filter worker start",
    "start:collector": "pnpm --filter collector start",
    "start": "pnpm -r --parallel start"
  },
  "author": "Guilherme Gules Moreira",
  "license": "ISC",
  "dependencies": {
    "pino": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.2",
    "tsconfig-paths": "^4.2.0"
  }
}
