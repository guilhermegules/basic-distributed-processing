{
  "name": "worker",
  "version": "1.0.0",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "shared": "workspace:*",
    "rabbitmq-client": "^5.0.5"
  }
}
